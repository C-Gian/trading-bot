# WP-005 checkpoint

Structural verdict: **PASS**. Completed on local `main`; no branch, history
rewrite, push, post-cutoff access, new strategy experiment, parameter search,
sealed evaluation, paper trade, Champion, or real-money feature was created.
Remote CI is **PENDING_PUSH**.

WP-004 is structurally accepted and ALIGNED remains **INCONCLUSIVE**. The two
accepted official raw archives reproduce all 21,602 off-grid timestamps and
payloads exactly, establishing `SOURCE_ARCHIVE_OFF_GRID_CONFIRMED`. The unchanged
quarantine reproduces 363 1h and 92 4h buckets with zero validation/warm-up
intersection and zero repair/fill.

The independent feature oracle and immutable trade/metric reconciliation pass.
All 12 WP-004 profiles replay to exact canonical content hashes. SEARCH_MEMORY_V2
now derives fingerprints from typed executable specs, binds admission to runtime
identity, retains nine legacy reference signatures, rejects falsified/manual
fingerprints, detects parameter drift, and prevents alias budget resets.

The exactly matched parent expectancies are DEFAULT `-0.0547388706 R`, ZERO
`+0.0653485444 R`, and DOUBLE `-0.1748262594 R`. ALIGNED's default delta is
`+0.1921323382 R`, above the frozen `0.12 R` hurdle. Across all 32 fixed random
gates, median expectancy is `+0.0269808015 R` and q90 is `+0.1107856627 R`;
ALIGNED's `+0.1373934676 R` exceeds q90. The diagnostic classification is
**ALIGNED_DIAGNOSTIC_SUPPORTED_BUT_INCONCLUSIVE**, not an independent p-value or
strategy approval.

Coverage is 48,784 common-eligible hours -> 1,875 raw parent breakouts -> 195 raw
ALIGNED candidates -> 125 signals while flat; 70 were suppressed by active
positions (35.8974%). All 125 attempts resolved. Fold sparsity, 3/6 nonnegative
folds, and >50% positive-fold profit concentration remain unresolved.

Scientific accounting remains experiments=9, new hypotheses=0, new variants=0,
numeric variants=0, diagnostic evaluations=35, replay profiles=12, cumulative
adaptive decisions/forks=2/2, sealed=0, paper=0, Champion=NONE, forward
evidence=NONE, real money=false. One mechanical post-execution aggregation failure
was preserved before the identical fixed diagnostic identities were rerun; it
finalized no result artifact and caused no result-dependent change.

Validation: 155 backend tests and 7 frontend tests pass with installed-data
provenance/oracle reconciliation, lint, format, types, and build. A detached clean
checkout passes 154 backend tests with one expected installed-data skip, 7 frontend
tests, bootstrap, lint, types, and build. Profitability is not a structural gate.

External housekeeping remains pending because authenticated repository
administration was not available; the remote default branch was not changed.

Next: Research Director review, then decide whether genuinely new evidence merits
a future explicit allocation; no Champion promotion or automatic FAM-BREAKOUT
reopening follows.
