# WP-016 preparation checkpoint

WP-016 is preregistered and available for Owner-initiated execution; no historical market
result was computed or observed in this checkpoint.

The official Wikimedia Pageviews API supplied 3,472 complete daily `en.wikipedia`
`Bitcoin` / `all-access` / `user` observations from 2015-07-01 through 2024-12-31. The
canonical reconstruction has zero missing, duplicate, or post-cutoff rows. A daily value
becomes eligible only at the UTC start of day D+2, implementing end-of-day D plus 24
hours. The sole added feature is the preregistered log ratio to the previous 28 complete
observations, excluding D.

SEARCH_MEMORY admitted `FAM-EXTERNAL-PUBLIC-ATTENTION`. The primary frozen HGBR adds the
attention shock to WP-015 F1-F8 plus settled funding; the matched control omits only the
attention value while requiring identical attention availability. Both use complete annual
folds 2020-2024, the 216-hour purge and outcome containment, threshold zero, and the four
fixed cost/timing profiles. Ten model fits are reserved and zero have executed.

Safe preregistration commit: `662176f`.

Research Lab presents `WP016_WIKIPEDIA_ATTENTION_V1` as the primary allowlisted candidate
and keeps WP-015 as reproduction history. The candidate binds exact preregistration
hashes, reports real deterministic stages, writes only local runtime artifacts, runs an
independent reconciliation, and generates the compact review bundle. Import and tests do
not invoke its real adapter.

Scientific state remains: 25 completed experiments, zero sealed queries, Champion NONE,
zero paper trades, ALIGNED unchanged, WP-009 paused, and real money false.
