# WP-006 checkpoint

Base reviewed HEAD: `444172a359e2663887624da82254cc2185ff85e1`

Structural verdict: **PASS**. Completed on local `main`; no branch, history rewrite,
push, post-cutoff access, sealed query, parameter search, paper trade, Champion or
real-money feature was created. Remote CI is **PENDING_PUSH**.

WP-005 is recorded as **ACCEPTED**. Its true starting HEAD is `3fdeffe5`, not the
`2b40aa03` printed in the WP-005 executor chat report; that non-material reporting
error is documented rather than corrected in place, and
`governance/WORK_PACKAGE_CHRONOLOGY.json` plus `validate_report_bases` now fail closed
if any work package from WP-006 onward claims a different base. Real GitHub Actions
after push succeeded for `444172a`: workflow `check`, run `34355806303`, conclusion
`success`, recorded in `reports/reviews/WP-005-CI-EVIDENCE.json`. Historical WP-005
artifacts keep their recorded `PENDING_PUSH` value and were not mutated.

`SEALED_EVALUATION_V1` was built **locked**, without acquiring, reading, summarizing
or querying any reserved BTCUSDT data. The BTC scope is `RESERVED_NOT_ACQUIRED` with
0 authorized and 0 consumed queries; `data/sealed/` does not exist. A request freezes
candidate, executable-spec, code, development-result, dataset, metric, profile and
budget identity; eligibility admits only `PROMISING_DEVELOPMENT_ONLY` with an explicit
Research Director allocation, so `INCONCLUSIVE` candidates — ALIGNED included — can
never query. Budget is consumed by an append-only ledger before any result exists, so
deleting a result never restores capacity. Twenty synthetic tests prove rejection of
unauthorized, ineligible, unallocated, drifted, mutated, duplicated, exhausted,
undeclared-metric and overwrite attempts, prove the development loader cannot open a
sealed path, and prove a consumed query cannot be reset. Isolation is documented as
application and repository layer, explicitly **not** OS-enforced.

The proposed `PERSISTENT_TREND_PULLBACK_RECOVERY_V1` family passed the governed
SEARCH_MEMORY_V2 novelty gate before any market result existed: `RECOVERY_CORE` is
`NEW_FAMILY` under the new root `FAM-PULLBACK-RECOVERY` with no matched legacy
experiment, and `RECOVERY_CONFIRM` is a `DESCENDANT_MECHANISM_CHANGE` within it. The
classifier was not weakened, nothing was renamed, and no budget was reset.
`FAM-BREAKOUT` remains exhausted at 4/4 configurations and 15/15 trials.

WP-006 records live in an additive V2 memory layer (ADR-0007) so the frozen V1
evidence and the byte-level WP-004 audit stay intact. Cumulative accounting sums both
layers; no counter was reset.

A pre-execution integrity correction was required and handled prospectively. The
preregistered executable-spec binding could not verify because the spec was stored as
JSON and compared against its dataclass form. Zero strategy trials and zero results
preceded the correction. The original preregistrations are preserved and superseded by
`preregistration.v2.json` under an immutable amendment registry that adds no variant,
profile or numeric search and leaves the hypothesis, primary metric, primary variant
and protocol unchanged.

Both authorized variants executed exactly their eight preregistered profile
evaluations under an unchanged `DEVELOPMENT_EVALUATION_V1`. `RECOVERY_CORE` is
**INCONCLUSIVE**: default `+0.0003823075 R`, zero-cost `+0.1204381257 R`, double-cost
`-0.1196734624 R`, delay `+0.0069160662 R`, 126 resolved trades, 3/6 nonnegative
folds, 8 trades in DEV-2022 against a required 15, and 68.6% positive-fold profit
concentration. `RECOVERY_CONFIRM` is also **INCONCLUSIVE**: default `+0.0219437002 R`,
103 trades against a required 120, and 7 in its worst fold. The family follows the
preselected primary, never the better-scoring variant. Every fold, profile and
negative result is retained.

Descriptively, the core rule sits above the matched parent breakout by
`+0.0551211781 R`, above the random-control mean by `+0.0971597682 R`, and below
ALIGNED by `-0.1370111601 R`. None of its 127 executed signals coincides with any
executed breakout or ALIGNED signal, which is consistent with a distinct mechanism but
does not establish one.

Sealed eligibility was assessed for all 11 development candidates: **zero** are
eligible. ALIGNED is `NOT_ELIGIBLE_INCONCLUSIVE` and both new variants are
`NOT_ELIGIBLE_INCONCLUSIVE`. No sealed query was executed.

Scientific accounting: experiments 11, economic hypotheses 4, configurations 11,
profile/seed trials 61, numeric parameter variants 0, adaptive decisions 3,
result-dependent forks 3, sealed evaluations 0, sealed queries 0, paper trades 0,
Champion NONE, forward evidence NONE, real money false.

Validation: 231 backend tests and 10 frontend tests pass with installed-data checks,
lint, format, types and build. Profitability is not a structural gate.

External housekeeping remains pending: the remote default branch is still
`work/wp-001-foundation-data` and no authenticated repository administration was
available without new credentials.

Next: Research Director review, then decide whether any family merits a further
explicit allocation. No Champion promotion and no automatic sealed unlock follows.
