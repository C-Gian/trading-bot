# ALIGNED development final closure + prospective shadow observer V1

Research Director acceptance is recorded: historical ALIGNED development is
`PARKED_DEVELOPMENT_SEARCH_EXHAUSTED`, while ALIGNED remains an unchanged BTCUSDT paper
research candidate. Historical discovery is paused; prospective evidence collection is
the primary phase.

`FUTURE_SHADOW_PAPER_EVIDENCE_V1` is implemented as a separate automated prospective
ledger with separate persisted health. It has no signal catch-up, a five-minute decision
window, durable-intent-first entry, one-position suppression, restart reconciliation only
for already-entered trades, frozen STOP_FIRST geometry/costs, and no order or credential
path. The product UI clearly labels it automated paper research with no real money.

Synthetic-clock/feed tests cover activation, NO_TRADE, LONG, strict future entry,
boundary-minute rejection, stop, target, expiry, restart, downtime, missed decisions,
and active-position suppression. No real observation was created by validation. Manual
paper V2 is hash- and behavior-preserved.

Accounting remains 26 historical experiments, 12 observed material historical
hypotheses, zero sealed queries, Champion `NONE`, and real money false.
