# WP-008 checkpoint

Base reviewed HEAD: `762b3b77f686305b1c73f19956d0b9b16b7a9b1c`

Structural verdict: **PASS**. WP-007 is independently recorded as accepted, including
real GitHub Actions `check` run `34375244463` at the exact reviewed head. Work completed
on local `main`; no branch, push, history rewrite, sealed/post-cutoff data access,
optimizer, model zoo, paper action, Champion, or real-money functionality occurred.
WP-008 remote CI remains **PENDING_PUSH**.

`RESEARCH_ARTIFACT_STORAGE_V1` applies prospectively. Historical evidence is unchanged.
WP-008 detailed rows are deterministic ZSTD Parquet with file and logical hashes;
training matrices/labels are not committed row by row, only compact regeneration
manifests.

`SUPERVISED_FEATURES_V1` implements the exact F1–F8 feature order and
`ISOLATED_FIXED_PLAN_DEFAULT_NET_R_V1`. Six expanding annual folds use training signals
strictly before validation start minus 216h, training-only `ddof=0` scaling, and label
outcomes strictly before validation. Invalid/unresolved labels are excluded and counted.
The minimum actual outcome-to-validation separation is 193 hours. Synthetic future
feature injections fail closed.

SEARCH_MEMORY_V2 admitted `LINEAR_FULL` as `NEW_FAMILY` under
`FAM-SUPERVISED-LINEAR`, and `LINEAR_NO_FLOW` as its structural ablation, before
results. Git proves implementation → admission/allocation → both preregistrations →
results. Exactly two configurations, four profiles each, twelve fold models, and zero
numeric, algorithm, hyperparameter, feature, or threshold variants were consumed.

All results are retained. `LINEAR_FULL` is **REJECT_COST_DOMINATED**: DEFAULT
`-0.1142218454 R`, ZERO `+0.0058246386 R`, DOUBLE `-0.2342683271 R`, DELAY_1H
`-0.1098336131 R`, 1,003 trades, 1/6 nonnegative folds, minimum fold 82, and ESS
852.75. `LINEAR_NO_FLOW` is also **REJECT_COST_DOMINATED**: DEFAULT
`-0.1172203904 R`, ZERO `+0.0028474849 R`, DOUBLE `-0.2372882645 R`, DELAY_1H
`-0.1041653216 R`, 969 trades, 1/6 nonnegative folds, minimum fold 72.

Independent reconciliation passed all twelve fits: boundaries, containment, feature
order, scaling, coefficients, prediction hashes, emitted timestamps, and DEFAULT
Parquet-derived metrics reproduce. FULL foldwise prediction/isolated-label correlations
range `-0.0330652157` to `+0.1090860500`; only 2023 is positive, and removing flow
changes DEFAULT by only `-0.0029985450 R`. The learned combination does not improve on
the strongest manual clue and is parked without rescue.

Scientific accounting: experiments 15, economic hypotheses 6, configurations 15,
profile/seed trials 77, supervised model fits 12, numeric variants 0, adaptive decisions
5, result-dependent forks 5, sealed evaluations/queries 0, paper trades 0, Champion
NONE, forward evidence NONE, real money false.

Sealed eligibility was regenerated for 15 candidates; neither linear configuration is
eligible. BTC remains 0 authorized / 0 consumed and `RESERVED_NOT_ACQUIRED`.

Validation: backend/frontend lint, format, typing, tests/build, installed-data replay,
independent model reconciliation, and clean-checkout `--no-data` validation pass.
Profitability is not a structural gate.

Next: Research Director review, then select only a genuinely distinct preregistered
direction under cumulative search accounting. No automatic sealed query or Champion
promotion follows.
