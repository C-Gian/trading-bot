# Prospective shadow evidence integrity hardening V1_1

Research Director acceptance of `PROSPECTIVE-SHADOW-PAPER-OBSERVER-V1` is recorded as
software status `ACCEPTED`. Historical ALIGNED development remains
`PARKED_DEVELOPMENT_SEARCH_EXHAUSTED` and the primary research phase remains
`PROSPECTIVE_EVIDENCE_COLLECTION`. V1 produced zero genuine prospective observations, so
`FUTURE_SHADOW_PAPER_EVIDENCE_V1` is recorded
`IMPLEMENTED_SUPERSEDED_BEFORE_FIRST_REAL_OBSERVATION` with nothing migrated and nothing
fabricated.

`FUTURE_SHADOW_PAPER_EVIDENCE_V1_1` and `PROSPECTIVE_SHADOW_PAPER_OBSERVER_V1_1` add
three integrity guarantees before the first genuine observation.
`BUILD_PROVENANCE_V1` binds each decision to a verified build: git HEAD, branch, clean
worktree, the application, observer, strategy, execution and cost-model versions, and a
manifest hashing the exact bytes of the observer, analysis, continuation, causal
execution engine, the cost implementation resolved from the imported objects, and this
contract. An unverified or dirty build evaluates no signal and lets the boundary become a
typed `UNVERIFIED_SCIENTIFIC_BUILD` missed decision. A single interprocess lease makes a
second scientific observer impossible rather than merely unlikely. A hash-linked
append-only audit chain shares one atomically replaced document with the snapshot and is
validated before evidence is used; failure degrades the observer and rewrites nothing.

Scientific semantics are untouched: ALIGNED, 2% stop, 4% target, 1,440-minute expiry,
`STOP_FIRST_V1`, `BTCUSDT_SPOT_COST_V1`, LONG/NO_TRADE, manual paper V2, and the
20-completed-trade freeze boundary are all unchanged and asserted by test.

Forty-three synthetic-clock and synthetic-feed observer tests cover dirty and unverified
builds, manifest identity, lease contention, genesis and chain extension, payload
modification, reordering, deletion, duplicate sequence, snapshot/audit divergence,
fail-closed behaviour without rewriting evidence, restart chain preservation, and the
production-write guards. No genuine future observation was created and the production
observer was never deliberately started.

Historical accounting remains 26 experiments, 12 observed material historical hypotheses,
zero sealed queries, Champion `NONE`, and real money false. Prospective counters remain
zero.
