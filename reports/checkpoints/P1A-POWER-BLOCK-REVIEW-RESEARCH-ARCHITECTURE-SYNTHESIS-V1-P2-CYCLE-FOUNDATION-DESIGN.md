# Checkpoint — P1A review, architecture synthesis, and P2 cycle foundation design

Starting HEAD: `70ff806e5352b0336c42c3272414adda1b7c822c` on local `main`.

## P1A disposition

The immutable power artifact was verified: MESI 24.0 bps/event, empirical MDE
285.640052 bps/event, power at MESI 0.01424888, and `REDESIGN_REQUIRED`.
`ALIGNED_SIGNAL_PERSISTS_TO_120H_V1` is `POWER_BLOCKED_NOT_EXECUTED`. No true zero-shift
outcome was computed or observed. This is not market `REJECT`, not market
`INCONCLUSIVE`, and consumes no observed material economic hypothesis. No ALIGNED rescue
or tuning is authorized.

## Architecture and source boundary

`research/design/RESEARCH_ARCHITECTURE_SYNTHESIS_V1.md` requires economic usefulness and
statistical resolvability before execution. Rare-event selectivity is not intrinsically
desirable when it destroys resolution. The listed single-algorithm modules remain empty
architecture slots; final algorithm components approved: NONE.

`docs/canonical/CYCLE_RESEARCH_SOURCE_BOUNDARY_V1.md` classifies public concepts as
`DOCUMENTED_PUBLIC`, project-completed rules as `RECONSTRUCTED`, and absent universal
executable detail as `UNSPECIFIED`. No project reconstruction is represented as an
official Marini rule.

## P2 frozen design

`BTC_TIME_CYCLE_STRUCTURE_V1` is the sole primary. It uses a 4h log-return context series
derived causally from canonical 1m development data, a fixed 2–90 day band, generalized
Lomb–Scargle estimation, a training-only AR-sieve/residual-block dependent null, and six
chronological outer folds. The two non-rescuing diagnostics are one frozen Bartels-style
quasi-persistence check and cross-fold period stability.

Prospective detectability repeats frequency selection inside every null/synthetic
replicate, injects cycles in log-price across frozen periods/phases and a structural SNR
grid, corrects the primary for frequency selection, and targets power 0.80. There is no
economic MESI for the structural test. Power prep must fail closed without exposing the
actual result if resolution is inadequate.

No swing, volume, inverse, vincolo, raccordo, target, cycle-based trading, cycle result,
or economic strategy was implemented or executed.

## Accounting and handoff

Unchanged: 26 completed experiments, 12 observed material economic hypotheses, zero
sealed queries, Champion NONE, real money false. P2 itself allocates one structural
primary, at most two diagnostics, and zero executed material economic hypotheses.

Next checkpoint: `P2-CYCLE-FOUNDATION-POWER-GATE-PREP`.
After P2 resolves: `RESEARCH-ARCHITECTURE-SYNTHESIS-V2` before another family.
