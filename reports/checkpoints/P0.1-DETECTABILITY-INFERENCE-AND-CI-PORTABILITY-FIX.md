# P0.1 Detectability Inference and CI Portability Fix

Status: **ACCEPTED**

The a8 Linux failure was isolated to ALIGNED's naive IID one-sided raw p-value, its
derived statistics hash, and the corresponding Markdown lines. The failed Linux run and
the local locked environment both used NumPy 2.4.6 and SciPy 1.17.1: the Student-t tail
differed by one final binary64 bit across platform builds.

Artifact reconstruction now uses platform-stable scalar reductions and a deterministic
80-digit Decimal Student-t tail calculation. Bootstrap and CI use pinned uv 0.12.10 with
`uv sync/run --frozen`; dependency identity is checked before audit reconstruction. No
precision tolerance was relaxed and no OS-specific expected artifact was introduced.

Positive-ACF trade ESS remains a diagnostic only. It cannot enter `compute_mde`; all 12
historical families now fail closed as `MDE_UNAVAILABLE` with
`NO_GOVERNED_INFERENTIAL_DEPENDENCE_MODEL`. No historical MESI or classification changed.

Exact correction head `292f3eb468e388a8844ed7431a24e4a446162e8c` passed GitHub Actions
run `34983918054`. Sealed queries remain zero and real-money authorization remains false.

Detailed evidence: `reports/reviews/P0.1-DETECTABILITY-CI-EVIDENCE.json`.
