# Prospective evidence live collection arm V1

Governance and state closure only. No scientific or product runtime source file changed,
the production observer was not started, live market data was not contacted, and no
genuine prospective observation was created.

Research Director acceptance is recorded: `PROSPECTIVE_SHADOW_PAPER_OBSERVER_V1_1` is
`ACCEPTED_EVIDENCE_READY` and `FUTURE_SHADOW_PAPER_EVIDENCE_V1_1` is
`ACCEPTED_FOR_PROSPECTIVE_COLLECTION`. The primary research phase remains
`PROSPECTIVE_EVIDENCE_COLLECTION`, historical discovery remains `PAUSED`, ALIGNED
development remains `PARKED_DEVELOPMENT_SEARCH_EXHAUSTED`, Champion remains `NONE`, and
real money remains false. `FUTURE_SHADOW_PAPER_EVIDENCE_V1` stays
`IMPLEMENTED_SUPERSEDED_BEFORE_FIRST_REAL_OBSERVATION` with zero real observations,
nothing migrated and nothing fabricated.

`PROSPECTIVE_COLLECTION_STATUS` is `ARMED_NOT_YET_OBSERVED`. The observer activates only
when the local backend starts, and a genuine first observation additionally requires a
verified clean build, the single observer lease, an hourly boundary strictly after
durable activation, and durable persistence inside the frozen five-minute window. No
backfill is possible. The validator asserts that neither the evidence store nor the
health store exists yet.

`PROSPECTIVE_COLLECTION_OPERATING_POLICY_V1` records that semantic code changes are not
made with the observer running, fixes the stop/preserve/validate/version/clean/restart
sequence for necessary engineering, and records that a dirty worktree is expected to fail
closed as `UNVERIFIED_SCIENTIFIC_BUILD` with its lost boundaries left missed.

The review boundary remains 20 completed automated shadow trades, recorded explicitly as
a minimum freeze boundary rather than proof of sufficiency, with no result-driven
adaptation before it and with ordinary `NO_TRADE` observations recorded as not justifying
adaptation. Early review is reserved for a persistently degraded observer, audit
integrity failure, suspected genuine evidence corruption, a semantic software bug, or any
proposed real-money boundary.

Historical accounting remains 26 experiments, 12 observed material historical hypotheses,
zero sealed queries, Champion `NONE`, and real money false. Prospective counters remain
separate and at zero.
